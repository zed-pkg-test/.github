{
  "name": "portable-node-cli-fixture",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "type": "commonjs",
  "bin": {
    "portable-node-cli": "./cli.js"
  },
  "scripts": {
    "format:check": "node --check cli.js && node --check test/cli.test.js && node --check scripts/run-ores-lint.cjs",
    "lint": "node --check cli.js && node --check test/cli.test.js && node --check scripts/run-ores-lint.cjs",
    "test": "node --test",
    "build": "node -e \"require('node:fs').accessSync('cli.js')\"",
    "lint:ores": "node scripts/run-ores-lint.cjs",
    "prepublishOnly": "npm run lint:ores",
    "prebuild": "npm run lint:ores"
  },
  "engines": {
    "node": ">=22"
  },
  "oresLint": {
    "note": "Declared, not installed. ores-lint resolves these from a global install - see .ores-lint/README.md",
    "requires": {
      "eslint": ">=9",
      "typescript-eslint": ">=8 (optional; without it .ts files are skipped)"
    },
    "install": "npm i -g eslint typescript-eslint"
  }
}
